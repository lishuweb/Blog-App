import base from './base';
import tags from './tags';
import paths from './paths';
import definitions from './definition';

export default {
    ...base,
    ...tags,
    ...paths,
    ...definitions,
};